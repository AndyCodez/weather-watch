<h1>Favorite Cities</h1>
<%if @cities.count > 0 %>
  <ul>
    <% @cities.each do |city| %>
      <li><%= city.name%></li>
    <% end %>
  </ul>
<% else %>
  <p>User has no favorite cities.</p>
<% end %>
